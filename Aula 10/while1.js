const cores = ["Branco", "Azul", "Amarelo", "Verde", "Roxo", "Rosa"];

let index = 0;

while (index < cores.length) {
  // length = Tamanho da lista

  console.log(index, cores[index]);

  index++; // index = index +1;
}
