// Criar uma lista com X números. Utilizando while e index retorne a posição e o conteúdo da lista.

const numeros = [17, 05, 1991, 27, 08, 22];

let index = 0;

while (index < numeros.length) {
  console.log(index, numeros[index]);
  index++; // index = index + 1
}
